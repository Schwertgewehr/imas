{
  "name": "Viewer",
  "version": "0.1.0",
  "description": "GUI for web browser",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "devDependencies": {
    "react": "x.x.x"
  }
}
